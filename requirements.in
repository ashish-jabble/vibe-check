# Production dependencies (top-level only). Compile with:
#     pip-compile requirements.in
# Do not edit requirements.txt by hand — it is generated.
#
# Floors document why a minimum version exists (CVEs, required features).
# pip-compile is free to pick newer patches; the lockfile pins the exact set.

flask>=3.1.0
flask-limiter>=3.8.0
requests>=2.32.0          # CVE-2024-35195 (Session.verify bypass) fixed in 2.32.0
beautifulsoup4>=4.13.0
lxml>=6.1.0               # multiple CVEs fixed in 6.x
playwright>=1.58.0
